[package]
name = "packet"
description = "The wire format, as one definition both ends compile."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
glam = { workspace = true }